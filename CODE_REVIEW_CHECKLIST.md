# Code Review Checklist — member-dashboard.html

**Дата начала:** 2026-02-25  
**Файл:** `dist/member-dashboard.html`, `src/js/member-dashboard.js`  
**Статус:** ✅ Фаза 1 завершена

---

## ✅ Выполнено (Фаза 1: Безопасность и Архитектура)

### 🔐 Безопасность

- [x] Заменить `onclick` в HTML на `addEventListener` (устранить inline-обработчики)
- [x] Добавить Content-Security-Policy (CSP) meta-тег
- [x] Добавить защиту от кликинг-атак (`referrer` policy)
- [x] Добавить sanitization пользовательских данных перед рендером (`sanitize()` в api.js)
- [x] Заменить localStorage на sessionStorage для токенов
- [x] **Создать `paymentService.js` для обработки платежей**
- [x] **Добавить валидацию данных карты на клиенте**
- [x] **Добавить mock endpoint для платежей (`/api/payments/process`)**
- [x] **Удалить hardcoded demo credentials из кода**
- [x] **Создать `.env.example` для environment variables**
- [x] **Добавить проверку ENABLE_DEMO_LOGIN для отключения в продакшене**
- [x] **Создать `validator.js` для валидации данных**
- [x] **Создать `toastManager.js` для уведомлений (замена alert)**
- [x] **Создать `stripe.js` для безопасной обработки платежей**
- [x] **Заменить raw card inputs на Stripe Elements**
- [x] **Создать `dom.js` для XSS защиты (sanitizeHTML, setTextContent)**
- [x] **Добавить проверку ролей на checkout (защита от неавторизованных)**
- [x] **Добавить проверку активной подписки на checkout**
- [ ] **Интегрировать платёжный шлюз** (Stripe/GoCardless) (когда будет бэк)
- [ ] **Добавить webhook для подтверждения платежей** (когда будет бэк)

### 🏗 Архитектура

- [x] Создать `src/js/services/api.js` — базовый HTTP-клиент с mock/real режимами
- [x] Создать `src/js/services/authService.js` — аутентификация и JWT
- [x] Создать `src/js/services/userService.js` — работа с профилем пользователя
- [x] Создать `src/js/services/orderService.js` — работа с заказами
- [x] Создать `src/js/services/subscriptionService.js` — работа с подписками
- [x] Заменить все `localStorage` вызовы на сервисные методы
- [x] Добавить обработку ошибок (401, 403, 404, 500)
- [x] Добавить JSDoc типизацию для всех сервисов

### 🔑 Аутентификация и авторизация

- [x] Добавить JWT-токены (mock-реализация для разработки)
- [x] Реализовать protected routes (редирект если нет токена)
- [x] Добавить refresh-механизм для токена (проверка exp)
- [x] Реализовать logout на клиенте
- [x] Добавить проверку роли пользователя (buyer/farmer)

### 📝 Валидация форм

- [x] Валидация email (формат)
- [x] Валидация телефона (формат)
- [x] Валидация ФИО (не пустое, мин. длина)
- [x] Валидация адреса (не пустое)
- [x] Отображение ошибок валидации в UI

### ♿ Доступность (Accessibility)

- [x] Проверить навигацию с клавиатуры (Tab, Enter, Escape)
- [x] Проверить ARIA-атрибуты на актуальность

---

## 🏗 Архитектура проекта

### ✅ Текущая структура (ОСТАВЛЯЕМ)

```
src/
├── *.html              # Страницы (index, member-dashboard, checkout, etc.)
├── js/
│   ├── modules/        # auth, cart, products, ui ✅
│   ├── utils/          # constants, storage, validation, dom ✅
│   ├── services/       # api, auth, user, order, payment, stripe ✅
│   └── data/           # farms, products (→ будут API) ✅
├── css/
└── assets/             # Картинки, иконки (можно добавить позже)
```

**Статус:** ✅ Работает хорошо, рефакторинг не нужен

### 📋 На будущее (когда будет бэкенд)

- [ ] Переместить HTML в `pages/`
- [ ] Добавить `js/index.js` (точка входа)
- [ ] Вынести assets в отдельную папку
- [ ] Рассмотреть TypeScript
- [ ] Добавить proper routing (если понадобится)

**Примечание:** Текущая структура достаточна для MVP. Рефакторинг рекомендуется только при значительном росте проекта.

---

## 📋 ПОШАГОВЫЙ ПЛАН (Pre-Backend)

**Документ:** [`PRE_BACKEND_CHECKLIST.md`](./PRE_BACKEND_CHECKLIST.md)

```
█████████████████████ 100% (11/11 задач)
```

| Этап | Статус | Задач | Прогресс |
|------|--------|-------|----------|
| Фаза 1: Безопасность | ✅ ЗАВЕРШЕН | 18/18 | 100% |
| **Фаза 2: Pre-Backend** | ✅ **ЗАВЕРШЕНА** | **11/11** | **100%** |
| Фаза 3: Backend Integration | ⏳ ОЖИДАЕТ | 0/15 | 0% |
| Фаза 4: Production Ready | ⏳ ОЖИДАЕТ | 0/8 | 0% |

**Текущая задача:** ✅ Фаза 2 ЗАВЕРШЕНА!  
**Следующий этап:** 🚀 Готовы к интеграции с бэкендом!  
**Блокеров:** нет

---

## 📋 Следующие шаги (Фаза 2: UI/UX и Тестирование)

### 🗄️ База данных (когда будет бэк)

- [ ] Выбрать БД (Supabase / PostgreSQL / MongoDB)
- [ ] Спроектировать схему БД (таблицы: users, orders, subscriptions, payments, reviews, products)
- [ ] Создать миграции для БД
- [ ] Настроить подключение к БД на бэкенде
- [ ] Реализовать API endpoints для CRUD операций
- [ ] Добавить синхронизацию между фронтом и БД
- [ ] Настроить бэкапы и восстановление данных
- [ ] Добавить индексы для оптимизации запросов
- [ ] Настроить транзакции для платежей
- [ ] Добавить soft delete для критичных данных

### 📡 API Endpoints (когда будет бэк)

- [ ] `POST /api/payments/create-intent` — создание Payment Intent для Stripe
- [ ] `POST /api/payments/webhook` — webhook от платёжной системы
- [ ] `GET /api/farmers/:id/products` — продукты фермера
- [ ] `POST /api/reviews` — создание отзыва
- [ ] `GET /api/reviews/farm/:id` — отзывы о ферме

### 🎨 UI/UX

- [ ] Добавить loading states (скелетоны или спиннеры)
- [ ] Добавить error states (сообщения об ошибках загрузки)
- [x] Добавить toast-уведомления для операций (успех/ошибка)
- [ ] Добавить подтверждение для критичных действий (удаление аккаунта)
- [ ] Проверить контрастность цветов (accessibility)
- [ ] Добавить focus states для всех интерактивных элементов
- [ ] Добавить pagination для списка заказов (если >10)

### ⚡ Производительность

- [ ] Добавить debounce для поисковых инпутов (если будут)
- [ ] Добавить lazy loading для изображений (если появятся)
- [ ] Добавить кэширование данных (React Query / SWR подход)
- [ ] Добавить virtual scroll для длинных списков (если нужно)

### 🧪 Тестирование

- [ ] Настроить Vitest + Testing Library
- [ ] Написать unit-тесты на сервисы (userService, orderService, etc.)
- [ ] Написать unit-тесты на validator.js
- [ ] Написать integration-тесты на API вызовы
- [ ] Написать E2E тесты на критичные сценарии:
  - [ ] Загрузка профиля
  - [ ] Обновление настроек
  - [ ] Просмотр заказов
  - [ ] Удаление аккаунта
  - [ ] Оплата подписки
- [ ] Добавить тесты на валидацию форм
- [ ] Настроить CI для запуска тестов

### 📅 Даты и время

- [ ] Добавить date-fns для работы с датами
- [ ] Исправить обработку временных зон
- [ ] Добавить форматирование дат (dd MMMM yyyy)
- [ ] Добавить локализацию дат (ru/en/cs)

### 📱 PWA (Progressive Web App)

- [ ] Добавить vite-plugin-pwa
- [ ] Создать manifest.json
- [ ] Настроить service worker
- [ ] Добавить offline режим
- [ ] Добавить push уведомления (опционально)

### 🌐 Интернационализация (i18n)

- [ ] Добавить i18next
- [ ] Создать locales/ru.json
- [ ] Создать locales/en.json
- [ ] Создать locales/cs.json
- [ ] Вынести все тексты в файлы переводов
- [ ] Добавить переключатель языков
- [ ] Проверить форматирование дат, валют, чисел
- [ ] Протестировать RTL языки (если нужно)

### 📦 Типизация и документация

- [ ] Добавить README с описанием структуры сервисов
- [ ] Документировать API endpoints (Swagger/OpenAPI)
- [ ] Добавить JSDoc для всех публичных функций
- [ ] Создать документацию для разработчиков

### 🗄️ Мониторинг и аналитика (перед запуском)

- [ ] Добавить Sentry / LogRocket для отслеживания ошибок
- [ ] Добавить Google Analytics / Plausible (опционально)
- [ ] Добавить метрики производительности (LCP, FID, CLS)
- [ ] Настроить алерты для критичных ошибок

### 🚀 Deployment (с бэкендом)

- [ ] Настроить CI/CD pipeline
- [ ] Добавить environment variables для API URL
- [ ] Настроить HTTPS для production
- [ ] Переключить API режим на 'real'
- [ ] Добавить VITE_STRIPE_PUBLIC_KEY для production

---

## 📊 Готовность к продакшену

| Компонент | Статус | Готовность |
|-----------|--------|------------|
| **Аутентификация** | ✅ Mock JWT | 90% |
| **Платежи** | ✅ Stripe Elements | 80% |
| **Валидация** | ✅ validator.js | 95% |
| **Безопасность** | ✅ CSP, sanitize | 90% |
| **UI/UX** | 🟡 Toast уведомления | 70% |
| **Тесты** | ❌ Нет | 0% |
| **Мониторинг** | ❌ Нет | 0% |
| **БД** | ❌ localStorage | 0% |
| **Бэкенд API** | ❌ Mock | 0% |
| **PWA** | ❌ Нет | 0% |
| **i18n** | ❌ Только русский | 10% |
| **Даты** | 🟡 Базовая обработка | 50% |

**Общая готовность:** ~45% (MVP готов, нужно: БД, тесты, мониторинг)

---

## 📊 Статистика изменений

| Файл | Было | Стало | Изменения |
|------|------|-------|-----------|
| `src/js/services/api.js` | 0 | 550+ строк | HTTP-клиент + payment endpoints |
| `src/js/services/authService.js` | 0 | 250+ строк | Аутентификация |
| `src/js/services/userService.js` | 0 | 200+ строк | Сервис профиля |
| `src/js/services/orderService.js` | 0 | 250+ строк | Сервис заказов |
| `src/js/services/subscriptionService.js` | 0 | 250+ строк | Сервис подписок |
| `src/js/services/paymentService.js` | 0 | 300+ строк | Сервис платежей |
| `src/js/services/stripe.js` | 0 | 300+ строк | **Stripe Elements интеграция** |
| `src/js/modules/auth.js` | 90 строк | 200 строк | Обновлён под сервисы |
| `src/js/member-dashboard.js` | 450 строк | 600 строк | Полностью переписан |
| `src/js/checkout.js` | 200 строк | 390 строк | Интеграция с paymentService + Stripe |
| `src/js/register.js` | 180 строк | 300 строк | Безопасность + env variables |
| `src/js/utils/validator.js` | 0 | 300+ строк | **Валидация данных (10+ методов)** |
| `src/js/utils/toast.js` | 0 | 180+ строк | **Toast уведомления** |
| `src/js/utils/dom.js` | 50 строк | 290 строк | **XSS защита + утилиты** |
| `dist/member-dashboard.html` | 1125 строк | 1150 строк | CSP + data-атрибуты + Toast CSS |
| `dist/checkout.html` | 450 строк | 500 строк | Stripe Elements контейнер |
| `.env.example` | 0 | 50 строк | Шаблон environment variables |
| `SECURITY.md` | 0 | 150 строк | Документ по безопасности |
| `.gitignore` | 15 строк | 35 строк | Защита .env файлов |

---

## 🎯 Ключевые улучшения

### Безопасность
1. **CSP** — Content-Security-Policy защищает от XSS атак
2. **Sanitization** — Все пользовательские данные очищаются перед выводом
3. **Token-based auth** — JWT токены вместо открытого localStorage
4. **No inline handlers** — Все обработчики через addEventListener
5. **Payment validation** — Валидация данных карты на клиенте
6. **Mock payment API** — Имитация платёжного шлюза для разработки
7. **Stripe Elements** — Безопасная обработка карт (PCI-DSS compliant)
8. **XSS защита** — sanitizeHTML, setTextContent, createElement

### Архитектура
1. **Сервисный слой** — Чёткое разделение ответственности
2. **Mock/Real режим** — Легкое переключение между разработкой и production
3. **Типизация JSDoc** — Лучшая поддержка IDE и документация
4. **Обработка ошибок** — Централизованная обработка API ошибок
5. **PaymentService** — Единый интерфейс для всех платёжных операций
6. **Validator** — Централизованная валидация форм

### Поддерживаемость
1. **Модульность** — Каждый сервис независим
2. **Документация** — JSDoc комментарии к функциям
3. **Константы** — Вынесенные конфигурации и статусы
4. **Environment variables** — Конфигурация через .env

---

**Дата обновления:** 2026-02-25  
**Сборка:** ✅ Успешна (~600ms)  
**Статус:** ✅ **Фаза 1: Безопасность и Архитектура — ЗАВЕРШЕНА**

---

## 📋 Что дальше?

### ✅ Завершено (Фаза 1)
- [x] Безопасность (CSP, XSS, sanitization)
- [x] Архитектура (сервисный слой)
- [x] Аутентификация (JWT mock)
- [x] Платежи (Stripe Elements)
- [x] Валидация (validator.js)
- [x] Toast уведомления
- [x] Проверка ролей на checkout

### 📌 Следующий этап (Фаза 2: UI/UX и Тесты) — **Важно**
- [ ] Loading states
- [ ] Error states
- [ ] Unit-тесты на сервисы
- [ ] E2E тесты

### 📅 Даты и PWA (Фаза 2.5) — **Средний приоритет**
- [ ] Добавить date-fns для работы с датами
- [ ] Добавить vite-plugin-pwa
- [ ] Создать manifest.json
- [ ] Настроить service worker

### 🌐 i18n (Фаза 3) — **Низкий приоритет (только если нужен мультиязык)**
- [ ] Добавить i18next
- [ ] Создать файлы переводов (ru/en/cs)

### 🕐 Когда будет бэк (Фаза 4)
- [ ] База данных (Supabase/PostgreSQL)
- [ ] API endpoints
- [ ] Интеграция с реальным Stripe
- [ ] Мониторинг (Sentry)

---

**Готовность к MVP:** ~45%  
**Готовность к продакшену:** ~0% (нужен бэкенд + БД + тесты)

---

## 🎯 Приоритеты задач

### 🔴 Критично (нужно перед продакшеном)
- [ ] База данных
- [ ] Бэкенд API
- [ ] Интеграция Stripe (real mode)
- [ ] Мониторинг ошибок (Sentry)

### 🟡 Важно (улучшает UX)
- [ ] Loading states
- [ ] Error states
- [ ] Unit-тесты
- [ ] Обработка дат (date-fns)

### 🟢 Опционально (по желанию)
- [ ] PWA (offline режим)
- [ ] i18n (если нужен мультиязык)
- [ ] Push уведомления

---

## 🗄️ Схема БД (для будущей реализации)

```sql
-- Users table
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    fullname VARCHAR(255) NOT NULL,
    phone VARCHAR(50),
    address TEXT,
    role VARCHAR(20) DEFAULT 'buyer',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Subscriptions table
CREATE TABLE subscriptions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    plan VARCHAR(50) NOT NULL,
    status VARCHAR(20) DEFAULT 'active',
    start_date TIMESTAMP DEFAULT NOW(),
    end_date TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Orders table
CREATE TABLE orders (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    total DECIMAL(10,2) NOT NULL,
    status VARCHAR(20) DEFAULT 'pending',
    delivery_date DATE NOT NULL,
    delivery_address TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Order items table
CREATE TABLE order_items (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    order_id UUID REFERENCES orders(id) ON DELETE CASCADE,
    product_id UUID NOT NULL,
    product_name VARCHAR(255) NOT NULL,
    quantity INTEGER NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    total DECIMAL(10,2) NOT NULL
);

-- Payments table
CREATE TABLE payments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    transaction_id VARCHAR(255) UNIQUE NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    currency VARCHAR(3) DEFAULT 'CZK',
    status VARCHAR(20) DEFAULT 'pending',
    payment_method VARCHAR(50) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Reviews table
CREATE TABLE reviews (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    farm_id UUID NOT NULL,
    rating INTEGER CHECK (rating >= 1 AND rating <= 5),
    comment TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Indexes for performance
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_subscriptions_user_id ON subscriptions(user_id);
CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_payments_user_id ON payments(user_id);
CREATE INDEX idx_reviews_farm_id ON reviews(farm_id);
```
