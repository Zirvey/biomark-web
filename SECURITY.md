# 🔐 BioMarket Security Checklist

## Критичные задачи перед продакшеном

### ✅ Выполнено

- [x] Content-Security-Policy (CSP) добавлен
- [x] Удалены inline `onclick` обработчики
- [x] Sanitization данных перед выводом
- [x] Token-based аутентификация (sessionStorage)
- [x] Environment variables для конфига
- [x] `.env` добавлен в `.gitignore`

### ⚠️ Требуется перед деплоем

#### 1. Отключить демо-режим

**Файлы:** `src/js/register.js`, `.env`

```bash
# В .env.production:
VITE_ENABLE_DEMO_LOGIN=false
```

**Проверка:**
```javascript
// Убедиться что в продакшене:
if (ENABLE_DEMO_LOGIN && import.meta.env.PROD) {
    // ❌ Это должно быть false!
}
```

---

#### 2. Удалить demo credentials

**Файлы:** `src/js/register.js`

```javascript
// ❌ УДАЛИТЬ ИЛИ ЗАКОММЕНТИРОВАТЬ:
const DEMO_CREDENTIALS = { ... };

// ✅ Оставить только для локальной разработки:
if (import.meta.env.DEV) {
    // demo credentials
}
```

---

#### 3. Настроить HTTPS

**Критично для:**
- Защиты токенов
- Безопасности платежей
- CORS policy

**Где:**
- Vercel (автоматически)
- Production сервер

---

#### 4. Настроить CORS

**Когда будет бэк:**

```javascript
// API должен разрешать только ваш домен
Access-Control-Allow-Origin: https://biomarket.cz
Access-Control-Allow-Credentials: true
```

---

#### 5. Проверить .env в git

```bash
# Убедиться что .env НЕ в git:
git ls-files | grep "\.env$"

# Должно вернуть пусто!
```

**Разрешено:**
- ✅ `.env.example` (шаблон без секретов)

**Запрещено:**
- ❌ `.env`
- ❌ `.env.production`
- ❌ `.env.local`

---

## Проверка перед деплоем

### 1. Audit зависимостей

```bash
npm audit
npm audit fix
```

### 2. Проверка CSP

Открыть DevTools → Console → Проверить что нет CSP violations

### 3. Проверка токенов

```javascript
// В консоли браузера:
sessionStorage.getItem('biomarket_token')
// ✅ Должен быть JWT токен
// ❌ Не должно быть паролей, полных данных карты
```

### 4. Проверка демо-доступов

Попробовать залогиниться с `admin@admin.com` / `admin@`

**Ожидаемый результат:** ❌ Должна быть ошибка

---

## Production Checklist

```
[ ] VITE_ENABLE_DEMO_LOGIN=false
[ ] VITE_API_MODE=real
[ ] VITE_API_URL=https://api.biomarket.cz
[ ] HTTPS настроен
[ ] CORS настроен на бэкенде
[ ] .env не в git
[ ] npm audit прошёл без critical уязвимостей
[ ] Демо-креды удалены или отключены
[ ] CSP работает без нарушений
```

---

## Контакты для безопасности

При обнаружении уязвимостей:
- Email: security@biomarket.cz (когда будет)
- GitHub Issues (private)

---

**Последнее обновление:** 2026-02-25  
**Статус:** ⚠️ Требуется отключение демо-режима перед продакшеном
